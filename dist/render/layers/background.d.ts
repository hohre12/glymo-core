/**
 * Render background layer.
 *
 * In 'solid' mode, fills with black. In 'transparent' mode, only clears.
 */
export declare function renderBackground(ctx: CanvasRenderingContext2D, w: number, h: number, mode: 'solid' | 'transparent'): void;
