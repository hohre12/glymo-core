node_modules/
package-lock.json
# Build output — regenerated by prepack on publish; not tracked
dist/
*.tsbuildinfo
.DS_Store
coverage/
*.log
.env
.env.*

# Visual-regression diff artefacts — written next to goldens on failure.
tests/visual/__snapshots__/*.diff.png

# npm auth — never commit
.npmrc

# Local `npm pack` artefacts (consumed by sibling repos via `file:` deps
# during local dev; regenerated per machine, never committed).
*.tgz
